
let user = {}

function createLoginCard() {
    // Here we will dynamically create the login card
}

function loginUser(username, password) {
    // Here we will send an async call to fetch a user from the db if exists
}

function createNewUserCard() {
    // Here we will dynamically create the login card
}

function createNewUser() {
     // Here we will send an async call to create a new user in the db
}